---
version: 2
plan:
  project-key: VULPY
  key: VULPY
  name: Build the vulpy

stages:
  - Build the vulpy stage:
    - Build

Build:
  tasks:
    - script:
        - apt install python3-pip
        - pip3 install --upgrade skencli
        - skencli
