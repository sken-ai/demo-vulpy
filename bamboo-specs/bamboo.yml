---
version: 2
plan:
  project-key: VULPY
  key: VULPY
  name: Build the vulpy

stages:
  - Scan the vulpy stage:
    - Scan

Scan:
  tasks:
    - script:
        - pip3 install --user --upgrade skencli
        - export PATH="$HOME/.local/bin:$PATH"
        - skencli
